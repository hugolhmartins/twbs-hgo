template(no-model)
	require(from="twbs-hgo/index as twbs" type="namespace")
	.test-loading-bar
		twbs:loading-bar(show="${true}")
		br
		twbs:loading-bar(show="${true}" bar-color="red")
		br
		twbs:loading-bar(show="${true}" bar-color="black")
		br
		twbs:loading-bar(show="${true}" bar-color="teal" background="#e5e5e5")
		alias